<div class="container" ng-show="isAuth">
    <nav class="row">
        <div class="col-md-2 logo col-sm-3 col-xs-4">
            <img src="~/Content/Images/logo-menu.PNG" />
        </div>

        <div class="user-menu">
            <div class="col-md-9 col-sm-8 col-xs-7">
                <ol class="breadcrumb relative">
                    <li ng-repeat="breadcrumb in breadcrumbs.get() track by breadcrumb.path" ng-class="{ active: $last }">
                        <a ng-if="!$last" ng-href="{{breadcrumb.path}}" ng-bind="breadcrumb.label" class="margin-right-xs"></a>
                        <span ng-if="$last" ng-bind="breadcrumb.label"></span>
                    </li>
                </ol>
            </div>

            <div class="col-md-1 col-sm-1 col-xs-1 pull-right" ng-if="!isPlugin">
                <button class="btn btn-default btn-sm pull-right" ng-click="signOut(this)">Sign Out</button>
            </div>
        </div>
    </nav>
</div>