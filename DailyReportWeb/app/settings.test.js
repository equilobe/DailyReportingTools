'use strict';

describe('app module', function () {

    beforeEach(module('app'));

    describe('PolicyCtrl controller', function () {

        it('should ....', inject(function ($controller) {
            var controller = $controller('PolicyCtrl');
            expect(controller).toBeDefined();
        }));

    });
});