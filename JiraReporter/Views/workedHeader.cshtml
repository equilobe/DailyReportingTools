@{

    var dayName = "";
}

    <i style="color:Sienna; font-size:12px; "> @Model.Date.DayOfWeek </i>
    @if (Model.TimeSpent > 0)
    {
        <i style="color:Sienna; font-size:12px;"> @Dot() @Model.TimeLogged worked </i>
    }
    else
    {
        <i style="color:orange; font-size:12px;"> @Dot() no work logged </i>
    }



