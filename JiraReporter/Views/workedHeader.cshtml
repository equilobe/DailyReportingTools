@{

    var dayName = "";
}

    <i style="color:Sienna;"> @Model.Date.DayOfWeek </i>
    @if (Model.TimeSpent > 0)
    {
        <i style="color:Sienna;"> @Dot() @Model.TimeLogged worked </i>
    }
    else
    {
        <i style="color:orange;"> @Dot() no work logged </i>
    }



