@Spacing(10,3)

<tr>
    <td colspan="3">
        <h3 style="color:black; margin-bottom:0px; padding-bottom:5px">Unassigned </h3>
    </td>
</tr>

@foreach (var task in Model.ReportTasks.UnassignedTasks)
{
    if (task.IsSubtask == false)
    {
        @Partial("Views/uncompletedTask.cshtml", task)
    }
}

